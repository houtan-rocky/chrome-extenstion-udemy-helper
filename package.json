{
  "scripts": {
    "start": "parcel watch manifest.json --host localhost --config @parcel/config-webextension --public-url ./",
    "build": "parcel build manifest.json --config @parcel/config-webextension --public-url ./",
    "generate-icons": "node generate-icons.js",
    "generate-store-assets": "node generate-store-assets.js"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.12.0",
    "process": "^0.11.10",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "parcel": "^2.12.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
